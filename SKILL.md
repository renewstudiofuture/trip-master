---
name: trip-master
description: 制定国内多日旅行攻略，或围绕已有交通、酒店和门票续排行程。公开信息通过搜索工具核实，仅用携程登录会话读取订单，自动进入订单中心；提供每日安排、关键衔接、预约清单、预算及Markdown/PDF。
---

# Trip Master

## 工作原则

- **公开信息直接搜索；登录浏览器仅用于携程订单读取。** 不要求登录高德、大众点评、飞猪，不打开这些平台的交互浏览器完成常规取数。携程新酒店候选也通过公开搜索，不以账户访问查询会员价。
- 不把公开搜索摘要当作实时余票、指定日期房价或已确认订单。公开工具支持时打开原文核对；搜索结果没有可靠数据，就保留精确缺口。
- 用户已登录携程时，自动复用会话并进入订单页，不让用户再手动点击“我的订单”。用户已打开页面而工具未发现标签时，进入“会话接续冲突”处理，不推断未登录、不直接新建页面。具体流程见 [携程订单读取](references/ctrip-orders.md)。
- 用户确认的景点日期、有效订单、平台排除项持续生效；后续章节不能覆盖这些约束。用户变更后只重算受影响部分。
- 只读订单及公开信息；不代为下单、改退、支付、领券或向店家发送消息。

## 1. 先识别任务，立即推进

分为三种情况：从零规划、已订行程续排、修改已有攻略。

先从当前对话及已有文件提取日期（含年份）、人数、预算、出发／返程城市、固定景点、订单所在平台。已有答案不再问。缺少非关键偏好时用合理且明示的假设继续；缺少决定交通的日期、起终点等关键条件时，只问尚缺的必要信息，同时开展独立工作。

**已订续排：先读取携程订单及用户已确认的门票，再填空白日期。** 不先提出会移动已订门票的替代行程。明确区分“未在携程发现”与“用户确认未订”。

多轮任务在客户工作目录维护一份简短的 `outputs/trip-state.md`，记录约束、有效订单、取消订单摘要、证据及待办；可沿用已有等价文件。不要为一次简单问答创建额外状态文件。订单字段与数据等级见 [证据与状态](references/evidence.md)。

取得关键约束后，先在对话给出已订摘要、每日骨架和最影响成行的缺口，随后继续核实；不要等所有餐厅、酒店候选都查完才呈现方案。

用户提供攻略链接、文字、截图或多位同伴意见时，读取 [行程组织与增量调整](references/itinerary-experience.md)：先提取候选地点和用户偏好，再核实；不要整篇照抄或把别人的行程当成用户已订事项。

## 2. 携程订单：一次进入，集中读取

仅当任务需要账户订单时读取 [ctrip-orders.md](references/ctrip-orders.md)。已打开并登录携程就自行进入订单中心；没有携程订单需求则完全跳过登录。

优先确认有效行程、机场／车站、入住退房、房间容量、付款状态和退改期限。列表已足够确认的字段不重复打开详情。取消订单不计入现行行程；不要为了“核实全部”遍历不相关历史订单。

## 3. 公开检索：优先解决成行条件

读取 [公开检索规则](references/public-research.md)，按以下顺序取数：

1. 交通衔接与退改时限：机场转站、车站到酒店、游览后赶车、返程。
2. 固定日期景点的预约与开放，核对公告适用日期。
3. 未订住宿：按连续入住段给位置与适配房型候选。
4. 就餐区域、菜式与可选餐厅；除非用户重视美食，不逐餐强制核评分。
5. 天气与装备：区别气候、近期天气和实际出行日预报。

使用搜索工具批量查询相互独立的问题，遵循工具查询数量上限。搜索结果对应的关键原文再集中打开；下一步依赖前一步结果时保持顺序。不要开启多个交互浏览器标签替代公开搜索。

每个问题先做一次聚焦查询和必要的原文核对；无有效结果时，换一个明确来源或更准确关键词补查一次。两轮仍没有新证据则标记缺口，继续其他事项；有实质新线索才继续追查。关键缺口必须出现在交付首页，不能藏在附录里。

## 4. 形成可执行安排与预算

- 日期固定后按顺路、体力和换乘安排，不强制塞满每天。三人家庭房不能仅凭名称认定容纳三位成人。
- 每天用统一顺序呈现：起点／住宿 → 交通 → 主要景点 → 就餐区域 → 终点／住宿；地点标为已锁定、建议或备选。未选中的收藏点不塞入正式日程。
- 临时增删景点时仅重算该日与相邻交通、住宿、预约、预算，保留无关日期；优先减少折返与紧张衔接，不声称算出了数学最优路线。
- 列出已核交通时刻、来源车程与计划缓冲，三者分栏。不能把缓冲时间写成地图实测，也不能由单向路线推定双向相同。
- 对未查到实时班次的段落给“建议查询时段＋所需衔接条件”，不编造航班／车次。不把站名相似的不同站当成同一处。
- 核心预算分为已支付、已订未付、当前可核报价、规划占位；汇总时同一项目只计一次。均价乘晚数明确标为估算，已购门票不要重复计入待支付金额。
- 对用户尚未明确的返程目的地、优惠资格等保留假设标记，不能默认为已确认。

## 5. 快速且诚实地交付

用户需要手机／微信点击跳转或反馈PDF链接不可用时，优先按 [手机交互版](references/mobile-web.md) 交付包豪斯风格响应式HTML；PDF作为离线备份。区分本地文件与已发布HTTPS链接，未经实际验证不称微信可用。

完整多日攻略默认输出 `outputs/<目的地>_攻略.md` 和同名PDF；用户只要简短修改或明确不要PDF时按要求处理。默认内容：行程总览、每日安排、关键待办／衔接、住宿与预算、来源及必要的行前提示。不强制12章、逐日重复酒店、祝语或地图。

最终PDF先按 [随身行程设计](references/pdf-design.md) 整理为日期导航、路线卡片与独立交通／住宿卡片；可用时应用 frontend-design 技能。默认使用 `scripts/build_cards_pdf.py`，完整来源及核实过程保留在配套Markdown，避免大段文字与整篇表格。按 [导出规则](references/export.md) 生成后渲染检查中文、卡片、跳转链接、页码及分页。导出本身不依赖登录浏览器。不因当前环境没有Edge而重新开发导出流程。

地图仅在用户要求且已有可靠坐标时制作；无法核实坐标时提供地点清单和导航入口。地图不能成为交付主攻略的前置条件。

交付前检查：固定日期未被改动；有效／取消订单分开；金额没有重复；信息有来源与适用日期；未查到的数据未写成已核实；关键交通和预约缺口醒目。只完成规划就说“规划版”，只有相关班次、票务和住宿条件确实核实后才能称“已核实可预订”。

公开分享手机网页时，按 [免费发布](references/publishing.md) 选择托管方式，只有部署成功并验证访问后才交付线上链接。

## 性能与用户协作

- 不承诺固定秒数；尽早提供可审阅的实质内容。进度更新说明新发现与剩余关键缺口，避免反复“正在核实”。
- 不把超时上限从30秒改60秒当作提速。减少调用次数、复用结果并及时停止无效路径，不能修改全局超时、网络或账户设置来掩盖故障。
- 仅携程订单路径可能请求用户登录或验证。其他公开信息缺失不转成多平台登录任务。
- 用户已授权查看订单，正常导航和只读展开无需再次征求许可。工具或权限有实质阻断时如实说明，不要求用户重复无助于解决故障的操作。

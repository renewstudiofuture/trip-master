---
name: trip-master
description: 根据自然语言或简问卷制作精简旅行网页，固定行程、景点、购物、餐饮、贴士五模块，含本地行程编辑、预算联动记账与重新规划入口；交通住宿仅整理用户提供信息，不登录或获取账号订单。
---

# Trip Master

用少量有效研究生成可持续更新的手机攻略。默认输出五模块包豪斯 HTML，可切换内置的“纸境拼贴”整套皮肤，PDF 按需。模板与脚本可复用，目的地事实不能照搬示例。

## 入口与骨架

自然语言信息充分就直接推进，不强制填问卷。需要补充时提供 `assets/questionnaire.html`，或运行 `scripts/build_intake.py <输出.html> --brief <已有需求.json>` 预填。问卷仅目的地、出发地、日期、预算、偏好；复制／下载交给 Agent，不会自动调用 AI。已有回答不重问。

日期可未定，预算可跳过。日期范围可推导天数；日期和天数都未知而逐日安排确实需要时才补问天数。人数、预算总额／人均与币种只在相关判断需要时补清，不能暗中假设年份、人数或返程目的地。

交通、住宿和已购门票只接收用户文字或截图。逐字段保留来源状态，模糊处待确认；不登录旅行平台、不获取账号订单，也不默认研究航班酒店候选。用户明确追加的其他任务另行处理，不把空白解释为没有预订。

用户不确定去哪时，由助手主动提出推荐，不要求用户先确定景点。骨架是中间步骤，确认后必须丰富成可游玩的攻略：每日给出推荐顺序、主体验、建议停留时长或时段、转场要点与一个必要的替代安排；推荐与未核实的营业信息分开标注，不因酒店未订而只写空泛占位。

先做少量地点／顺路／明显闭馆核查，给简洁逐日骨架，确认后再深入研究与生成。已有同轮确认持续有效；用户明确要求直接生成时跳过草案确认。固定日期和用户提供的有效预订不能悄悄改动。

## 研究：先筛选，只查缺口

用户希望参考小红书热门路线时，使用 [小红书高赞研究](references/xiaohongshu.md)：使用内置提取代码与现有浏览器工具，先应用站内“最多点赞”并确认生效，再读取结果；不依赖 AutoCLI 程序或额外专用扩展，再用官方资料核实。连接失败不反复轮换关键词，不把普通搜索结果冒充高赞笔记。

依照 [公开研究](references/public-research.md) 研究入选地点：真实名称／分店、位置与坐标、开放预约、推荐理由、对应图片；评分可选。查询条件需包括适用日期。图片／评分缺失不阻塞交付，不能编造补足。

独立问题批量查询；一次原文提取多个字段；五模块引用同一地点记录。一个缺口通常聚焦查一次，必要时换明确来源补一次；无新证据停止，保留失败原因。出现新线索或日期／条件变化才针对性重查。

不强制八模块、地点数量、每日逐店评分或全部候选配图。先筛选，再详细核实，最后只处理入选图片。默认依靠搜索与公开原文，不把公共研究变成浏览器登录任务。

## 统一数据与交付

生成前按 [数据契约](references/data-model.md) 建立 `trip.json`。这是唯一行程事实源；五模块、网页、可选PDF均从它派生。`trip_id` 和地点／活动ID保持稳定，内容变化递增 `revision`。预算估算与实际账本独立。

```sh
python scripts/trip_model.py validate outputs/trip.json
python scripts/build_mobile_html.py outputs/trip.json outputs/mobile/index.html
```

固定内容：行程（含用户提供交通住宿和规划预算）、景点、购物、餐饮、贴士。核心景点说明独特吸引力、2–3个具体看点和建议走法，配已核实主体与使用许可的实景图并注明拍摄季节/历史图片。不能用“看湖看山”“散步休息”代替推荐理由，也不能以图多代替信息可靠性。辅助点无需凑图。每个地点保留到访要点；不凑栏目数量。不购物可直接注明未安排。日期／导航／复制地址／记账／调整／分享按照 [手机交互](references/mobile-web.md) 使用已有组件，不每次重写代码。

渲染前解决数据冲突，缺资料保留待确认；自动校验不能证明外部事实正确。渲染后检查手机宽度、日期锚点和变化的交互，完整步骤见 [交付检查](references/export.md)。外链图片与地图仍需网络，不把静态生成等同地图或图片离线可用。

静态网页不绑定作者的电脑、账号或托管平台。按用户要求分享时，遵循内置的 [发布流程](references/publishing.md)：优先使用当前使用者指定或已授权的托管服务，已有旅行发布记录则核验后更新原网址；无可用服务时引导一次必要授权。已有发布工具或技能可复用，但不要求安装其他技能。未请求发布不上传。已有授权沿用，不增加无必要的确认往返。公开版本不能包含原始截图、账号／证件／真实订单号或签名链接。

## 内置视觉风格

使用者只安装 Trip Master。纸境拼贴的转绘与网页设计规则内置于 [视觉风格](references/visual-style.md)，生成或修改该皮肤时读取；不得要求安装 RedSkill、photo-to-holly06-papercollage 或其他风格技能，不把外部技能名称当成执行依赖。

默认包豪斯；底部“换皮肤”切换整页视觉与目的地首页插画，复用同一行程和账本。封面根据本次目的地已核验的实景照片转绘，景点详情继续展示实景。仅使用当前环境可用的生图能力；缺少能力时保留可用实景封面并如实提示，不冒充已生成插画，也不自动引导安装扩展。

复用已生成资产，不因切换皮肤、刷新、记账或修改文字重新生成图片。必须实际实现并检查页面切换后才能声称换肤可用；仅更新技能规则不等于已更新网页。

## 修改与续作

静态网页支持本地行程编辑：增删、改名/说明、跨天移动与同日顺序调整即时更新并保存到当前浏览器。公共攻略保持原版，分享链接不携带个人调整。行程和账本独立导入/导出；版本不匹配不得静默覆盖旧调整，保留导出与恢复入口；锁定交通、预订需交给助手处理。

需要新地点研究或整体重排时，用户在独立“需要助手重新规划”入口复制要求和个人行程回聊天。该入口不调用AI、不自动发消息。旧版 operations 申请仍可用 `scripts/trip_model.py request` 检查，但新版个人行程包是独立格式，应先比较 base_revision 和锁定事项，不把它直接当旧申请执行。

先检查版本和固定事项，再判断影响范围：文案改动不搜索；删点复核前后衔接；跨天移动同时检查两天；日期、城市变化扩大核实范围。自由意见必须先理解再结构化，不照搬脚本的初步范围。锁定项目只有用户明确要求变更后才更新源数据，不代为改退。

只更新事实源并重建受影响输出。局部改动不重搜无关日期；用 [状态与证据](references/evidence.md) 的短记录续作，不反复加载整份原文、历史对话或全部 references。

## 本地记账与完成边界

记账、待办保存到当前浏览器，以稳定 `trip_id` 隔离；更新攻略版本不清空账本。提供编辑、删除、导出／导入，重复导入去重，冲突整批拒绝。待办实质变化需重新确认。没有云同步、分摊或自动汇率换算；分享链接不共享账本。

每个面向用户的阶段结束时，用简短提示说明：已完成什么、推荐下一步、在哪里如何操作，以及可跳过或结束的方式。只提示当前阶段需要的动作，不把内部工具步骤变成用户确认。已授权且不依赖回答的工作直接继续，不为提示新增确认关卡。

引导面向使用者，不是工具日志：每一步只推荐一个主要动作，用1–3句自然语言交代结果与去向，不用“下一步继续？”反复索取已经给过的授权。需要回复时明确说“直接在当前聊天回复”；文字/截图交到当前聊天，网页按钮只做实际具备的操作，不能暗示网页会自动联系助手。长时间研究、生成或发布时主动简短报进度；遇到等待或失败说明卡点及可继续的路径，不让用户猜是否停住。

可沿用的引导示例（按实际阶段改写，不逐项机械输出）：
- 骨架完成：“每日路线已整理。直接在当前聊天回复‘满意’，或告诉我想调整的那一天；确认后我会补充玩法并生成网页。”
- 收集交通住宿：“已有信息已放进行程。还有预订可直接发文字或截图；没有就回复‘跳过’，我会保留待确认并继续。”用户已说没有新信息时省略这一步。
- 网页完成：“点击预览查看，可试试换肤、返回行程和记账。修改意见直接发在当前聊天；满意后可结束，或告诉我‘发布’生成分享网址。”已有发布授权则直接发布，不再催确认。
- 发布完成：“已更新到这个网址：[实际链接]。现在可以转发同行者；建议用手机打开检查，打不开就把提示发回这里。”任务已完成时允许自然结束，不人为追加流程。

阶段衔接：需求足够→生成骨架；骨架获确认→研究与生成；可选交通住宿缺失→允许补充或跳过；用户说“跳过”“没有新信息”→保留待确认并进入网页验收，不再催补；网页交付→提供可打开链接及明确的“满意／修改”回复方式；用户确认满意→验收完成，本地交付可结束，公开发布仅作为可选下一步，已有发布授权则执行，否则不能把满意当成发布授权；发布成功→给出分享链接与打开检查提示，失败→说明原因和可执行下一步。

网页通过 trip.json 的 delivery_stage 保留当前阶段引导，禁止只改生成HTML导致重建回退：文字或截图发回生成攻略的聊天，助手重规划请求需复制后回聊天发送，不能暗示网页自动上传或联系助手。引导应随当前阶段更新；收集结束后不再把补资料作为必经步骤。

总预算与账本应始终联动展示：总预算、同币种已记支出、剩余/超出预算、使用比例；零账目不意味着实际零消费。其他币种单列，不混算。账目采用紧凑行，次要编辑/删除操作折叠，保留触控尺寸。

实际支出不会自动继承规划费用，按币种分别合计；余额只按已记录消费计算。存储失败要明确提示并导出，不能伪报保存。迁移域名／设备用导出导入。

先验证关键日期、用户预订、衔接、费用与图片主体，再评价速度。工具等待和 token 不凭空估计；无本任务计量就记未测。只加载当前阶段需要的文件和记录；按实际变化重跑必要检查，不以减少验证冒充提速。
